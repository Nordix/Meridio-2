apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: e2e-dual-stack

resources:
- ../../../../config/default

images:
- name: controller-manager
  newName: registry.nordix.org/cloud-native/meridio-2/controller-manager
  newTag: latest

nameSuffix: -dual-stack

labels:
- pairs:
    e2e-suite: dual-stack

patches:
- patch: |-
    apiVersion: admissionregistration.k8s.io/v1
    kind: ValidatingWebhookConfiguration
    metadata:
      name: not-important
      annotations:
        cert-manager.io/inject-ca-from: e2e-dual-stack/meridio-2-serving-cert-dual-stack
    webhooks:
    - name: vl34route-v1alpha1.kb.io
      namespaceSelector:
        matchLabels:
          e2e-suite: dual-stack
      clientConfig:
        service:
          name: meridio-2-webhook-service-dual-stack
          namespace: e2e-dual-stack
  target:
    kind: ValidatingWebhookConfiguration
- patch: |-
    - op: replace
      path: /spec/dnsNames
      value:
      - meridio-2-webhook-service-dual-stack.e2e-dual-stack.svc
      - meridio-2-webhook-service-dual-stack.e2e-dual-stack.svc.cluster.local
    - op: replace
      path: /spec/secretName
      value: webhook-server-cert-dual-stack
  target:
    group: cert-manager.io
    kind: Certificate
    name: serving-cert
- patch: |-
    apiVersion: apps/v1
    kind: Deployment
    metadata:
      name: not-important
    spec:
      template:
        spec:
          containers:
          - name: manager
            env:
            - name: MERIDIO_LB_SERVICE_ACCOUNT
              value: meridio-2-stateless-load-balancer-dual-stack
          volumes:
          - name: webhook-certs
            secret:
              secretName: webhook-server-cert-dual-stack
  target:
    kind: Deployment
    labelSelector: control-plane=controller-manager
