apiVersion: meridio-2.nordix.org/v1alpha1
kind: DistributionGroup
metadata:
  name: dg-eps
spec:
  type: Maglev
  maglev:
    maxEndpoints: 8
  selector:
    matchLabels:
      app: target-eps
  parentRefs:
  - name: gw-eps
